# 🚗 ร้านแต่งรถ - โปรเจกต์ฐานข้อมูลหลายฐาน

แอปพลิเคชัน Full Stack ที่ใช้ Flask backend พร้อม MySQL และ MongoDB ด้วยคอนเทนเนอร์ Docker

## 📋 โครงสร้างโปรเจกต์

```
├── backend/                 # แอปพลิเคชัน Flask
│   ├── app.py              # Flask หลัก
│   ├── db_mysql.py         # การเชื่อมต่อ MySQL
│   ├── db_mongo.py         # การเชื่อมต่อ MongoDB
│   ├── config.py           # การกำหนดค่า
│   ├── requirements.txt     # การพึ่งพา Python
│   ├── Dockerfile          # Backend container
│   ├── sql/                # สคริปต์การเตรียมใช้งานฐานข้อมูล
│   └── .env                # ตัวแปร Environment
├── frontend/               # TypeScript + React + Vite
│   ├── src/                # React components
│   ├── package.json        # การพึ่งพา Frontend
│   └── Dockerfile          # Frontend container
├── docker-compose.yml      # การจัดการ Multi-container
├── .gitignore              # กฎ Git ignore
├── DOCKER.md               # เอกสารการตั้งค่า Docker
├── start.bat               # สคริปต์เริ่มต้น Windows
└── start.sh                # สคริปต์เริ่มต้น Linux/Mac
```

## 🎯 เริ่มต้นอย่างรวดเร็ว

### ตัวเลือกที่ 1: การตั้งค่าอัตโนมัติ (แนะนำ)

**Windows:**
```bash
start.bat
```

**Linux/Mac:**
```bash
chmod +x start.sh
./start.sh
```

### ตัวเลือกที่ 2: การตั้งค่าด้วยตนเอง

1. **โคลนและนำทาง:**
```bash
cd Ziing_multi_database_project
```

2. **คัดลอกไฟล์ Environment:**
```bash
cp .env.example .env
cp backend/.env.example backend/.env
```

3. **เริ่มต้นบริการ:**
```bash
docker-compose up -d
```

4. **ตรวจสอบสถานะ:**
```bash
docker-compose ps
```

## 🌐 เข้าถึงบริการ

| บริการ | URL | ข้อมูลประจำตัว |
|---------|-----|-------------|
| Backend API | http://localhost:5000 | - |
| Frontend | http://localhost:5173 | - |
| MySQL | localhost:3306 | ผู้ใช้: shopuser, รหัสผ่าน: shoppass |
| MongoDB | localhost:27017 | ผู้ใช้: admin, รหัสผ่าน: adminpass |

## 📡 จุดปลายทาง API

```
GET  /                    - ตรวจสอบสุขภาพ
GET  /customers           - รับลูกค้าทั้งหมด
POST /customers           - สร้างลูกค้า
GET  /vehicles            - รับยานพาหนะทั้งหมด
GET  /workorders          - รับคำสั่งงานทั้งหมด
GET  /parts               - รับชิ้นส่วนทั้งหมด (MongoDB)
POST /parts               - สร้างชิ้นส่วน (MongoDB)
```

## 🐳 คำสั่ง Docker

```bash
# ดูบันทึก
docker-compose logs -f backend

# เข้าถึง MySQL
docker-compose exec mysql mysql -u shopuser -pshoppass CarCustomShop

# เข้าถึง MongoDB
docker-compose exec mongodb mongosh admin --username admin --password adminpass

# หยุดบริการ
docker-compose down

# ล้างข้อมูลทั้งหมด (รวมถึงข้อมูล)
docker-compose down -v
```

## 📝 การกำหนดค่า

### ตัวแปร Environment

แก้ไขไฟล์ `.env` เพื่อปรับแต่ง:

```env
MYSQL_HOST=mysql
MYSQL_PORT=3306
MYSQL_USER=shopuser
MYSQL_PASSWORD=shoppass
MYSQL_DB=CarCustomShop

MONGO_HOST=mongodb
MONGO_PORT=27017
MONGO_DB=CarCustomShop

FLASK_ENV=development
FLASK_DEBUG=1
```

## 🚀 การพัฒนา

### การพัฒนา Backend

การเปลี่ยนแปลงโค้ดจะสร้างใหม่โดยอัตโนมัติ ดูบันทึก:
```bash
docker-compose logs -f backend
```

### การพัฒนา Frontend

React components สร้างใหม่ผ่าน Vite การเปลี่ยนแปลงจะแสดงอยู่ในทันที

```bash
docker-compose logs -f frontend
```

## 📚 เอกสารเพิ่มเติม

ดู [DOCKER.md](DOCKER.md) สำหรับคู่มือการตั้งค่า Docker โดยละเอียด การแก้ไขปัญหา และคำแนะนำการปรับใช้องค์กร

## 🔧 Stack เทคโนโลยี

**Backend:**
- Python 3.11
- Flask 2.3
- PyMySQL (ไดรเวอร์ MySQL)
- PyMongo (ไดรเวอร์ MongoDB)

**Frontend:**
- React 18
- TypeScript
- Vite
- Node.js 18

**ฐานข้อมูล:**
- MySQL 8.0
- MongoDB 7.0

**โครงสร้างพื้นฐาน:**
- Docker
- Docker Compose

## 📄 ใบอนุญาต

โปรเจกต์นี้เป็นส่วนหนึ่งของโปรเจกต์กลุ่ม โปรดอ้างอิงแนวทางของสถาบัน

## 👥 ทีม

สร้างด้วย Docker เพื่อการรวมฐานข้อมูลหลายฐานแบบไร้รอยต่อเพื่อ local

---

สำหรับความช่วยเหลือเพิ่มเติม ดู [DOCKER.md](DOCKER.md) หรือติดต่อหัวหน้าทีม
