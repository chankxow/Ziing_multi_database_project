# 🚗 ระบบนิเวศประสิทธิภาพรถยนต์ - ฐานข้อมูลรวมสำหรับห่วงโซ่อุปทานและบันทึกการสร้างมืออาชีพ

## ภาพรวมโครงการ

นี่คือแอปพลิเคชัน Full-Stack ที่พัฒนาขึ้นสำหรับจัดการร้านแต่งรถยนต์ด้วยเทคโนโลยีสมัยใหม่:
- **Backend**: Flask (Python)
- **ฐานข้อมูล**: MySQL + MongoDB (Localhost)
- **Frontend**: React + TypeScript + Vite
- **โครงสร้างพื้นฐาน**: Local Development (ไม่มี Docker)

### วัตถุประสงค์
จัดการข้อมูลลูกค้า ยานพาหนะ ชิ้นส่วน และคำสั่งงาน โดยใช้ฐานข้อมูลที่ปรับให้เหมาะสมกับแต่ละประเภทข้อมูล พร้อมระบบจัดการห่วงโซ่อุปทานและบันทึกการสร้างรถยนต์แบบมืออาชีพ

---

## 🚀 เริ่มต้นอย่างรวดเร็ว

### ✅ การตั้งค่าการพัฒนาในพื้นที่ (Standalone)

**ข้อกำหนดเบื้องต้น:**
- Python 3.8+
- Node.js 16+
- MySQL Server (ทำงานบน localhost:3306)
- MongoDB (ทำงานบน localhost:27017)

### 📋 คำแนะนำการติดตั้ง

**ขั้นตอนที่ 1: เริ่มต้นฐานข้อมูล**
```bash
# ตรวจสอบว่า MySQL และ MongoDB กำลังทำงานอยู่
# MySQL: localhost:3306
# MongoDB: localhost:27017
```

**ขั้นตอนที่ 2: ติดตั้งฐานข้อมูลที่ปรับปรุง**
```bash
# MySQL - สร้างและนำเข้าฐานข้อมูล
mysql -u root -p -e "CREATE DATABASE CarCustomShop;"
mysql -u root -p CarCustomShop < backend/sql/init.sql
mysql -u root -p CarCustomShop < backend/sql/enhanced_schema.sql

# MongoDB - สร้าง collection (ตัวเลือก)
mongosh --eval "use CarCustomShop; db.createCollection('parts');"
```

**ขั้นตอนที่ 3: เริ่มต้น Backend**
```bash
cd backend
pip install -r requirements.txt
python app.py
# Backend ทำงานที่ http://localhost:5000
```

**ขั้นตอนที่ 4: เริ่มต้น Frontend**
```bash
cd frontend
npm install
npm run dev
# Frontend ทำงานที่ http://localhost:5173
```

### การเข้าถึงบริการ
- **Backend API**: http://localhost:5000
- **Frontend**: http://localhost:5173
- **MySQL**: localhost:3306
- **MongoDB**: localhost:27017

---

## 📦 โครงสร้างโครงการ

```
Ziing_multi_database_project/
├── backend/                    # Flask Backend
│   ├── app.py                 # ไฟล์แอปพลิเคชันหลัก
│   ├── auth.py                # ระบบตรวจสอบสิทธิ์ JWT
│   ├── config.py              # การตั้งค่าสภาพแวดล้อม
│   ├── db_mysql.py            # ตัวเชื่อมต่อ MySQL
│   ├── db_mongo.py            # ตัวเชื่อมต่อ MongoDB
│   ├── requirements.txt        # การพึ่งพา Python
│   ├── sql/                   # สคริปต์เริ่มต้นฐานข้อมูล
│   │   ├── init.sql           # ฐานข้อมูลพื้นฐาน
│   │   └── enhanced_schema.sql # ฐานข้อมูลที่ปรับปรุง
│   ├── .env                   # ตัวแปรสภาพแวดล้อม (ไม่ติดตาม)
│   └── .env.example           # ไฟล์ตัวอย่างสภาพแวดล้อม
│
├── frontend/                   # React + TypeScript + Vite
│   ├── src/                   # คอมโพเนนต์ React
│   ├── public/                # ไฟล์สถิต
│   ├── index.html             # ไฟล์ HTML หลัก
│   ├── package.json           # การพึ่งพา Node.js
│   ├── vite.config.ts         # การตั้งค่า Vite
│   ├── tsconfig.json          # การตั้งค่า TypeScript
│   └── .env                   # ตัวแปรสภาพแวดล้อม
│
├── README.md                  # เอกสารภาษาอังกฤษ
├── README_TH.md              # เอกสารภาษาไทย (นี่)
├── Documentation.md           # เอกสารประกอบฉบับเต็ม
├── .gitignore                 # กฎการละเว้น Git
└── .env.example               # ไฟล์ตัวอย่างสภาพแวดล้อม

```

---

## 🗄️ ฐานข้อมูล

### MySQL - ข้อมูลเชิงสัมพันธ์ (localhost:3306)
ใช้สำหรับ:
- **Customers**: ข้อมูลส่วนตัว ข้อมูลติดต่อ
- **Vehicles**: รุ่น ปี ทะเบียนรถ
- **WorkOrders**: รายละเอียดงาน สถานะ ต้นทุน
- **Users**: ผู้ใช้และบทบาท (admin, mechanic, customer, supplier)
- **Suppliers**: ข้อมูลผู้จัดจำหน่าย
- **BuildProjects**: โครงการสร้างรถ
- **BuildStages**: ขั้นตอนการสร้าง
- **PerformanceData**: ข้อมูลสมรรถนะ
- **InventoryTransactions**: ธุรกรรมสินค้าคงคลัง

### MongoDB - ข้อมูลแบบยืดหยุ่น (localhost:27017)
ใช้สำหรับ:
- **Parts**: ข้อมูลชิ้นส่วน ราคา สินค้าคงคลัง

---

## 📡 API Endpoints

### การตรวจสอบสิทธิ์
| วิธี | Endpoint | คำอธิบาย |
|--------|----------|-----------|
| POST | `/auth/login` | เข้าสู่ระบบ |
| POST | `/auth/register` | สมัครสมาชิก |
| GET | `/auth/me` | ข้อมูลผู้ใช้ปัจจุบัน |

### จัดการโครงการสร้าง
| วิธี | Endpoint | คำอธิบาย |
|--------|----------|-----------|
| GET | `/build-projects` | ดูโครงการทั้งหมด |
| POST | `/build-projects` | สร้างโครงการใหม่ |
| GET | `/build-stages/<project_id>` | ดูขั้นตอนโครงการ |
| POST | `/build-stages` | สร้างขั้นตอนใหม่ |

### จัดการผู้จัดจำหน่าย
| วิธี | Endpoint | คำอธิบาย |
|--------|----------|-----------|
| GET | `/suppliers` | ดูผู้จัดจำหน่ายทั้งหมด |
| POST | `/suppliers` | เพิ่มผู้จัดจำหน่ายใหม่ |

### จัดการชิ้นส่วน (MySQL)
| วิธี | Endpoint | คำอธิบาย |
|--------|----------|-----------|
| GET | `/parts-mysql` | ดูชิ้นส่วนทั้งหมด |
| POST | `/parts-mysql` | เพิ่มชิ้นส่วนใหม่ |

### ข้อมูลสมรรถนะ
| วิธี | Endpoint | คำอธิบาย |
|--------|----------|-----------|
| GET | `/performance-data/<project_id>` | ดูข้อมูลสมรรถนะโครงการ |
| POST | `/performance-data` | เพิ่มข้อมูลสมรรถนะใหม่ |

### API พื้นฐาน
| วิธี | Endpoint | คำอธิบาย |
|--------|----------|-----------|
| GET | `/` | ตรวจสอบสถานะเซิร์ฟเวอร์ |
| GET | `/customers` | ดูลูกค้าทั้งหมด |
| POST | `/customers` | เพิ่มลูกค้าใหม่ |
| GET | `/vehicles` | ดูยานพาหนะทั้งหมด |
| GET | `/workorders` | ดูคำสั่งงานทั้งหมด |
| GET | `/parts` | ดูชิ้นส่วนทั้งหมด (MongoDB) |
| POST | `/parts` | เพิ่มชิ้นส่วนใหม่ (MongoDB) |

---

## 🔐 ระบบความปลอดภัย

- **อย่าแชร์ไฟล์ .env** ในการควบคุมเวอร์ชัน
- **เปลี่ยนรหัสผ่าน** ในการตั้งค่าสภาพแวดล้อม
- **อย่าฝังข้อมูลรับรอง** ในโค้ด
- ใช้ `.gitignore` เพื่อป้องกันไม่ให้ติดตามไฟล์ที่ละเอียดอ่อน

### บทบาทผู้ใช้
- **admin**: เข้าถึงได้ทั้งหมด
- **mechanic**: จัดการโครงการ ชิ้นส่วน ข้อมูลสมรรถนะ
- **customer**: ดูโครงการของตนเองเท่านั้น
- **supplier**: จัดการชิ้นส่วนและข้อมูลผู้จัดจำหน่าย

---

## 🔧 การตั้งค่า

### ไฟล์ .env สำหรับตัวแปรสภาพแวดล้อม
```env
# การตั้งค่า Flask
FLASK_ENV=development
FLASK_DEBUG=1
FLASK_APP=app.py

# การตั้งค่า MySQL (Localhost)
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=your_mysql_password
MYSQL_DB=CarCustomShop

# การตั้งค่า MongoDB (Localhost)
MONGO_HOST=localhost
MONGO_PORT=27017
MONGO_DB=CarCustomShop
```

---

## 🚗 คุณสมบัติหลักของระบบนิเวศ

### 🏗️ การจัดการโครงการสร้าง
- ติดตามความคืบหน้าแบบเรียลไทม์
- จัดการขั้นตอนการสร้างและการพึ่งพา
- บันทึกเวลาและต้นทุน
- เอกสารประกอบด้วยรูปภาพ/วิดีโอ

### 📦 การจัดการห่วงโซ่อุปทาน
- จัดการผู้จัดจำหน่ายและการสั่งซื้อ
- ติดตามสินค้าคงคลังแบบอัตโนมัติ
- การแจ้งเตือนเมื่อสินค้าใกล้หมด
- การจัดการราคาและต้นทุน

### 📊 การวิเคราะห์สมรรถนะ
- บันทึกข้อมูล Dyno, แทร็ก, ควอเตอร์ไมล์
- กราฟและการวิเคราะห์แนวโน้ม
- การเปรียบเทียบประสิทธิภาพ
- รายงานสมรรถนะแบบ PDF

### 👥 พอร์ทัลลูกค้า
- ติดตามสถานะโครงการแบบเรียลไทม์
- อนุมัติงานและการเปลี่ยนแปลง
- ดูเอกสารและรูปภาพความคืบหน้า
- การสื่อสารกับทีมช่าง

---

## 🤝 การมีส่วนร่วม

1. สร้างสาขาคุณสมบัติ: `git checkout -b feature/your-feature`
2. คอมมิทการเปลี่ยนแปลง: `git commit -m 'เพิ่มคุณสมบัติบางอย่าง'`
3. พุชไปยังสาขา: `git push origin feature/your-feature`
4. เปิด Pull Request

---

## 👥 สมาชิกทีม

- **Chankxow**: นักพัฒนา
- **puriwat2953**: นักพัฒนา
- **Ampser**: นักพัฒนา

---

## 💡 หมายเหตุ

โครงการนี้สร้างขึ้นเพื่อการศึกษาเพื่อทำความเข้าใจ:
- ระบบฐานข้อมูลหลายชนิด
- เทคโนโลยี Docker & Container
- การพัฒนา Full-Stack
- Python Flask Framework
- React TypeScript Development
- ระบบจัดการห่วงโซ่อุปทาน
- การบันทึกการสร้างรถยนต์แบบมืออาชีพ

---

## 📞 การสนับสนุน

หากคุณมีคำถามหรือปัญหา:
1. ตรวจสอบ logs ของ Backend/Frontend terminals
2. ตรวจสอบว่าบริการทั้งหมดทำงานอยู่
3. สร้าง issue พร้อมคำอธิบายรายละเอียด

---

**อัปเดตล่าสุด**: กุมภาพันธ์ 2026  
**เวอร์ชัน**: 1.0.0 - Automotive Performance Ecosystem  
**สถานะ**: ✅ พร้อมใช้งานและพัฒนาต่อ
